FROM node:20-alpine

WORKDIR /app

# tzdata so TZ=America/... resolves for log timestamps
RUN apk add --no-cache tzdata

COPY package.json package-lock.json* ./
RUN npm install

# Source is mounted as a volume in docker-compose
CMD ["npm", "run", "dev"]
